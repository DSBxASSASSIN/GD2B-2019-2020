const ctx = document.querySelector('canvas').getContext('2d');

ctx.canvas.width = document.documentElement.clientWidth;
ctx.cancas.height = document.documentElement.clientHeight;

function Update(){
    requestAnimationFrame(Update);
    ctx.
}